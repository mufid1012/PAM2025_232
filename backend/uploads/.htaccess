# Allow direct access to image files
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# Allow all file types in this directory
<FilesMatch "\.(jpg|jpeg|png|gif|webp)$">
    Require all granted
</FilesMatch>
